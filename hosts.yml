all:
  children:
    prod:
      vars:
        github_account: chriszal
      hosts:
        my-host:
          ansible_host: 35.241.238.99
          # apps must have the same name as the corresponding Github repo
          apps:
            - referenceLetterApp
            - referenceLetterAppPanel
